{% extends site.template~'/base.html.twig' %}

{% block title %}{{ page.page.metaTitle|default }}{% endblock %}
{% block metaDescription %}{{ page.page.metaDescription|default }}{% endblock %}
{% block metaKeywords %}{{ page.page.metaKeywords|default }}{% endblock %}
{% block metaAuthor %}{{ page.page.metaAuthor|default }}{% endblock %}

{% block body %}

    <!-- header -->
    {% include site.template~'/header.html.twig' %}

    <div class="container body-container">
        <main id="content">
            <div class="row">
                <div class="col-12 col-md-4">
                    <h1>search filters</h1>
                </div>
                <div class="col-12 col-md-8">
                    {% for item in search %}
                        <div class="row pb-3">
                            <div class="col-4">
                                {% set image = item.images|first %}
                                {% if image|first is not empty %}
                                    <img src="{{ image|first }}" class="img-fluid img-thumbnail">
                                {% endif %}
                            </div>
                            <div class="col-8">
                                <p class="">
                                    <a href="{{ path('default', {'slug':item.slug}) }}">
                                        {{ item.title }}
                                    </a>
                                </p>
{#                                <p class="text-success">#}
{#                                    <span class="fa fa-home" aria-hidden="false"></span> /#}
{#                                    {% for breadCrumb in item.breadCrumbs %}#}
{#                                        {% if breadCrumb.slug is not defined %}#}
{#                                            <span>{{ breadCrumb.title|raw }}</span> /#}
{#                                        {% else %}#}
{#                                            <span>#}
{#                                            {% if loop.last %}#}
{#                                                <a href="{{ path('default', {'slug':breadCrumb.slug}) }}" class="text-success">{{ breadCrumb.title|raw }}</a>#}
{#                                            {% else %}#}
{#                                                <a href="{{ path('default', {'slug':breadCrumb.slug}) }}" class="text-success">{{ breadCrumb.title|raw }}</a> /#}
{#                                            {% endif %}#}
{#                                        </span>#}
{#                                        {% endif %}#}
{#                                    {% endfor %}#}
{#                                </p>#}
                                <p class="text-muted">
                                    {{ item.banner|striptags }}
                                    {{ item.description|striptags }}
                                </p>
                            </div>
                        </div>
                        <hr>
                    {% endfor %}

                </div>
            </div>
        </main>
    </div>

    <!-- footer -->
    {% include site.template~'/footer.html.twig' %}

{% endblock %}
