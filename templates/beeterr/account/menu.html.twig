<ul class="list-group d-none d-sm-block">
    <a href="{{ path('default', {'slug':'cart'}) }}" class="list-group-item d-flex justify-content-between align-items-center ">
        {{ 'Mijn winkelmand'|trans }}
    </a>
    <a href="{{ path('account_user', {'_locale':app.request.locale}) }}" class="list-group-item d-flex justify-content-between align-items-center ">
        {{ 'Mijn gegevens'|trans }}
    </a>
    <a href="{{ path('account_orders', {'_locale':app.request.locale}) }}" class="list-group-item d-flex justify-content-between align-items-center">
        {{ 'Mijn bestellingen'|trans }}
        <span class="badge badge-success">{{ user.orders|length }}</span>
    </a>
    <a href="{{ path('account_shipping_addresses', {'_locale':app.request.locale}) }}" class="list-group-item d-flex justify-content-between align-items-center">
        {{ 'Mijn Adressen'|trans }}
        <span class="badge badge-success">{{ user.addresses|length }}</span>
    </a>
    <a href="{{ path('logout') }}" class="list-group-item list-group-item">

        <span class="fa fa-sign-out"></span>
        {{ 'Uitloggen'|trans }}
    </a>
</ul>
